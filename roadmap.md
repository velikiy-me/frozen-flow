* implement support of JWT authorization mocking with actual attributes
* implement support of dependencies between body and headers in all combinations
* implement custom pattern matcher for relative dates
* support arbitrary wiremock json patterns
* refactor request pattern processor to extract pattern creation logic into separate non-mime specific classes
* implement custom stub format, proxying/recording and loading into mappings in playback mode
